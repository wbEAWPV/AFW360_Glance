# Runtime dependencies for the AFW 360 At A Glance Shiny app.
#
# Target interpreter: Python 3.11.9, pinned in .python-version.
# Connect offers 3.8.18 / 3.9.13 / 3.11.9 and matches on major-minor, so a
# newer local minor fails the deploy outright:
#   "Cannot find compatible environment: no compatible Local environment
#    with Python version 3.13.7"
# Build the venv with `uv venv` from this directory and it picks 3.11.9 up.
# Deployed to Posit Connect; rsconnect builds the bundle manifest from this file.
#
# Deliberately NOT here:
#   geopandas / shapely / pyproj  - only scripts/build_geojson.py needs them, and it is
#                                   run offline. geo/*.json is committed, so the running
#                                   app never touches GDAL/GEOS/PROJ. See plan section 6.
#   pytest, rsconnect-python      - development only, see requirements-dev.txt.
shiny==1.8.0
shinywidgets==0.8.1
plotly==7.1.0
pandas==3.0.6
openpyxl==3.1.5
faicons==0.2.2
